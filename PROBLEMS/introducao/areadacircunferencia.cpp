#include <bits/stdc++.h>
using namespace std;

int main() {
    double raio;
    cin >> raio;
    double area;
    area = 3.1416 * raio * raio;
    cout << fixed << setprecision(2) << area << endl;
}
